---
description: Build, vet, lint, and test all modules
agent: build
---
Run `task check` to build, vet, lint, and test all modules.
If any step fails, analyze the errors and fix them.
After fixing, run `task check` again to verify.
