version: "2"

run:
  go: "1.26"

linters:
  default: standard
  enable:
    - gocritic
    - revive
    - misspell
    - errorlint
  disable:
    - unused
  settings:
    revive:
      rules:
        - name: exported
          disabled: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
